% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tensor_mle.R
\name{tensor_mle}
\alias{tensor_mle}
\title{tensor_mle}
\usage{
tensor_mle(
  draws,
  max_iter = 1000,
  tol = 0.01,
  quiet = TRUE,
  model,
  restrict = NULL,
  penalize_nu = FALSE,
  nu_prior_shape = 2,
  nu_prior_rate = 0.05
)
}
\arguments{
\item{draws}{An array containing the draws, where the first mode represents
each draw.}

\item{max_iter}{A max number of iterations to try to get covariance matrices
that converge.}

\item{tol}{A tolerance level to define the convergence of matrices.}

\item{model}{Specify the distribution of draws. Must be one of normal, skewt,
vargamma, invgauss, or gen hyper}

\item{penalize_nu}{Logical. For the skewt model, should a shifted gamma
prior penalty be applied to the degrees of freedom?}

\item{nu_prior_shape, nu_prior_rate}{Shape and rate for the shifted gamma
prior \code{nu - 4 ~ Gamma(shape, rate)} used when \code{penalize_nu}
is \code{TRUE}.}
}
\value{
A list containing the estimated mean array and the list of covariance
matrices.
}
\description{
Estimates the mean array and covariance matrices from an array of tensor
variate draws. If tensor variate normal, uses the flop-flop algorithm. Otherwise,
uses the expectation-maximization algorithm.
}
