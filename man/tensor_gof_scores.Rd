% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tensor_gof_scores.R
\name{tensor_gof_scores}
\alias{tensor_gof_scores}
\title{Fast score-adjusted Rosenblatt goodness-of-fit test}
\usage{
tensor_gof_scores(
  draws,
  model = c("normal", "skewt", "vargamma", "invgauss", "genhyper"),
  fit = NULL,
  marginal_degree = 2L,
  include_interactions = TRUE,
  max_basis = 100L,
  calibration = c("multiplier", "chisq"),
  B = 1999L,
  seed = NULL,
  multiplier_chunk_size = 250L,
  eigen_tol = 1e-08,
  max_iter = 1000L,
  tol = 1e-06,
  quiet = TRUE,
  restrict = NULL,
  rel.tol = 1e-07,
  abs.tol = 1e-09,
  subdivisions = 100L
)
}
\arguments{
\item{draws}{A \code{tensor} object containing IID observations.}

\item{model}{The fitted distribution: \code{"normal"}, \code{"skewt"},
\code{"vargamma"}, \code{"invgauss"}, or \code{"genhyper"}.}

\item{fit}{An optional parameter list returned by \code{\link[=tensor_mle]{tensor_mle()}}. When
\code{NULL}, the model is fitted once.}

\item{marginal_degree}{Positive integer giving the largest marginal
shifted-Legendre degree.}

\item{include_interactions}{Whether to include pairwise first-degree
interaction terms.}

\item{max_basis}{Maximum number of basis functions.}

\item{calibration}{P-value calibration. \code{"multiplier"} uses a fast
score-adjusted weighted bootstrap and is the default. \code{"chisq"} uses the
experimental asymptotic chi-squared calibration.}

\item{B}{Number of weighted-bootstrap replications when
\code{calibration = "multiplier"}.}

\item{seed}{Optional random seed for the weighted bootstrap.}

\item{multiplier_chunk_size}{Number of multiplier replications processed
in one matrix multiplication. This controls temporary memory use without
changing the result.}

\item{eigen_tol}{Relative eigenvalue threshold used for pseudoinverses and
numerical ranks.}

\item{max_iter, tol, quiet, restrict}{Arguments passed to \code{\link[=tensor_mle]{tensor_mle()}} when
\code{fit} is \code{NULL}. Restricted covariance fits are not currently supported.}

\item{rel.tol, abs.tol, subdivisions}{Quadrature controls passed to
\code{\link[=tensor_rosenblatt]{tensor_rosenblatt()}}.}
}
\value{
An object inheriting from \code{"htest"}. Its \code{statistic} and \code{p.value}
entries contain the smooth statistic and its selected calibrated p-value.
The former covariance-whitened statistic and chi-squared p-value are
retained as \code{asymptotic.statistic} and \code{asymptotic.p.value}.
}
\description{
Performs a smooth goodness-of-fit test on tensor Rosenblatt values while
accounting for parameters estimated from the same observations. This
version uses \code{\link[=tensor_scores]{tensor_scores()}} rather than parameter-by-parameter finite
differences.
}
\details{
If \eqn{h_i} denotes the selected Rosenblatt basis vector and \eqn{s_i}
the fitted-model score, this function constructs the adjusted influence
rows
\deqn{g_i=h_i-CI^+s_i,}
where \eqn{C=Cov(h_i,s_i)} and \eqn{I=Var(s_i)}. For the default weighted
bootstrap, the observed statistic is \eqn{n\|\bar h\|^2}. Each null
realization is obtained from
\deqn{\left\|n^{-1/2}\sum_i(Z_i-\bar Z)g_i\right\|^2,}
where the \eqn{Z_i} are independent standard-normal multipliers. This
requires no new tensor draws, model fits, or Rosenblatt transformations.

The weighted bootstrap is a large-sample calibration and should still be
validated for a proposed model and sample-size regime. The chi-squared
calibration is retained mainly for comparison.
}
\examples{
draws <- rtnorm(
  100,
  mu = array(0, dim = c(2, 2)),
  sigmas = list(diag(2), diag(2))
)
result <- tensor_gof_scores(
  draws,
  model = "normal",
  marginal_degree = 2,
  max_basis = 10,
  B = 999,
  seed = 1
)
result$statistic
result$p.value

}
