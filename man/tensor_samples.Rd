% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tensor_samples.R
\name{tensor_samples}
\alias{tensor_samples}
\title{Construct a sample of tensor-valued observations}
\usage{
tensor_samples(x, obs = 1L)
}
\arguments{
\item{x}{An array containing identically shaped tensor observations.}

\item{obs}{A single numeric or character location identifying the
observation mode Defaults to the first mode}
}
\value{
A \code{tensor_samples} object backed by an array with
observations stored along its first mode
}
\description{
\code{tensor_samples()} stores identically shaped tensor observations in one
array. The selected observation axis is moved to the first mode; the
remaining modes describe the shape of each observation.
}
\examples{
input <- array(1:24, dim = c(3, 4, 2))
x <- tensor_samples(input, obs = 3)
n_draws(x)
draw_shape(x)
pull_draw(x, 1)
}
