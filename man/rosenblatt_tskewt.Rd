% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rosenblatt_tskewt.R
\name{rosenblatt_tskewt}
\alias{rosenblatt_tskewt}
\title{Rosenblatt transform for tensor-variate skewed-t observations}
\usage{
rosenblatt_tskewt(
  draws,
  mu,
  skew,
  sigmas,
  nu,
  rel.tol = 1e-07,
  abs.tol = 1e-09,
  subdivisions = 100L,
  show_progress = FALSE
)
}
\arguments{
\item{draws}{A \code{tensor} object containing one or more observations.}

\item{mu}{A tensor/array containing the location parameter.}

\item{skew}{A tensor/array containing the skew parameter.}

\item{sigmas}{A list containing one covariance matrix for each tensor mode.}

\item{nu}{The positive skewed-t degrees-of-freedom parameter.}

\item{rel.tol}{Relative tolerance passed to \code{\link[stats:integrate]{stats::integrate()}}.}

\item{abs.tol}{Absolute tolerance passed to \code{\link[stats:integrate]{stats::integrate()}}.}

\item{subdivisions}{Maximum number of subintervals used by
\code{\link[stats:integrate]{stats::integrate()}}.}

\item{show_progress}{Whether to report progress after each tensor draw.}
}
\value{
A \code{tensor} object with the same draw shape and draw count as
\code{draws}. Under the model with known parameters, its entries are
independent Uniform(0, 1) variables in R's column-major tensor order.
}
\description{
Convenience wrapper around \code{\link[=tensor_rosenblatt]{tensor_rosenblatt()}} for tensor-variate
skewed-t observations.
}
\details{
This wrapper constructs the skewed-t parameter list and calls
\code{\link[=tensor_rosenblatt]{tensor_rosenblatt()}}. See that function for the conditional GIG
quadrature and fitted-parameter caveat.
}
\examples{
set.seed(1)
draws <- rtskewt(
  n = 5,
  mu = array(0, dim = c(2, 2)),
  skew = array(0.2, dim = c(2, 2)),
  sigmas = list(diag(2), diag(2)),
  nu = 6
)
uniforms <- rosenblatt_tskewt(
  draws,
  mu = array(0, dim = c(2, 2)),
  skew = array(0.2, dim = c(2, 2)),
  sigmas = list(diag(2), diag(2)),
  nu = 6
)
uniforms
}
