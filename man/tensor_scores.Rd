% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tensor_scores.R
\name{tensor_scores}
\alias{tensor_scores}
\title{Per-observation scores for fitted tensor distributions}
\usage{
tensor_scores(
  draws,
  model = c("normal", "skewt", "vargamma", "invgauss", "genhyper"),
  fit = NULL,
  max_iter = 1000L,
  tol = 1e-06,
  quiet = TRUE,
  restrict = NULL
)
}
\arguments{
\item{draws}{A \code{tensor} object containing IID observations.}

\item{model}{The fitted distribution: \code{"normal"}, \code{"skewt"},
\code{"vargamma"}, \code{"invgauss"}, or \code{"genhyper"}.}

\item{fit}{An optional parameter list returned by \code{\link[=tensor_mle]{tensor_mle()}}. When
\code{NULL}, the model is fitted once.}

\item{max_iter, tol, quiet, restrict}{Arguments passed to \code{\link[=tensor_mle]{tensor_mle()}} when
\code{fit} is \code{NULL}. Restricted covariance fits are not currently supported.}
}
\value{
A numeric matrix with one row per observation and one column per
identifiable fitted parameter. The fitted parameters and model are
available in the \code{"fit"} and \code{"model"} attributes.
}
\description{
Computes one likelihood-score row for each tensor observation. For the
normal model the scores are analytic. For the normal variance-mean mixture
models, the function uses Fisher's identity and one evaluation of the
conditional mixing-variable moments at the supplied fit. It does not
perturb and re-evaluate every model parameter.
}
\details{
The covariance factors use the same identifiable working parameterization
as \code{\link[=tensor_score_gof]{tensor_score_gof()}}: Cholesky coordinates, with unit mean diagonal for
all covariance factors except the last. Shape parameters constrained to be
positive are represented on the log scale.

The returned scores are evaluated at \code{fit}. Consequently, column means
should be close to zero when \code{fit} is a sufficiently converged maximum
likelihood estimate.
}
\examples{
draws <- rtnorm(
  50,
  mu = array(0, dim = c(2, 2)),
  sigmas = list(diag(2), diag(2))
)
fit <- tensor_mle(draws, model = "normal")
scores <- tensor_scores(draws, model = "normal", fit = fit)
dim(scores)

}
