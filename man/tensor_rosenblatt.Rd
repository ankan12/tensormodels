% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tensor_rosenblatt.R
\name{tensor_rosenblatt}
\alias{tensor_rosenblatt}
\title{Rosenblatt transform for tensor-variate distributions}
\usage{
tensor_rosenblatt(
  draws,
  model = c("normal", "skewt", "vargamma", "invgauss", "genhyper"),
  parameters,
  rel.tol = 1e-07,
  abs.tol = 1e-09,
  subdivisions = 100L,
  show_progress = FALSE
)
}
\arguments{
\item{draws}{A \code{tensor} object containing one or more observations.}

\item{model}{One of \code{"normal"}, \code{"skewt"}, \code{"vargamma"}, \code{"invgauss"},
or \code{"genhyper"}.}

\item{parameters}{A named list of model parameters. The output of
\code{\link[=tensor_mle]{tensor_mle()}} can be passed directly. Every model requires \code{mu} and
\code{sigmas}; skewed models also require \code{skew}. In addition, \code{"skewt"}
requires \code{nu}, \code{"vargamma"} requires \code{gamma} (or \code{scale}),
\code{"invgauss"} requires \code{kappa}, and \code{"genhyper"} requires \code{lambda} and
\code{omega}.}

\item{rel.tol}{Relative tolerance passed to \code{\link[stats:integrate]{stats::integrate()}} for skewed
models.}

\item{abs.tol}{Absolute tolerance passed to \code{\link[stats:integrate]{stats::integrate()}} for skewed
models.}

\item{subdivisions}{Maximum number of subintervals used by
\code{\link[stats:integrate]{stats::integrate()}} for skewed models.}

\item{show_progress}{Whether to report progress after each tensor draw.}
}
\value{
A \code{tensor} object with the same draw count and draw shape as
\code{draws}. With known parameters under the selected model, its entries are
independent Uniform(0, 1) variables in R's column-major tensor order.
}
\description{
Applies a sequential Rosenblatt transformation to observations from any
distribution supported by \code{\link[=tensor_mle]{tensor_mle()}}. The tensor normal transformation
uses exact modewise Cholesky innovations. The four skewed distributions use
deterministic adaptive quadrature over their updated generalized inverse
Gaussian mixing distributions.
}
\details{
For a skewed model, modewise whitening gives
\deqn{Y = W a + \sqrt{W} Z,}
where the entries of \eqn{Z} are independent standard normals and the
initial mixing distribution is \eqn{GIG(\lambda_0,\chi_0,\psi_0)}.
After conditioning on the first \eqn{r} vectorized entries,
\deqn{\lambda_r = \lambda_0-r/2,\quad
      \chi_r = \chi_0+\sum_{j=1}^r y_j^2,\quad
      \psi_r = \psi_0+\sum_{j=1}^r a_j^2.}
The next conditional CDF is the expectation of
\eqn{\Phi\{(y_{r+1}-W a_{r+1})/\sqrt{W}\}} under this updated GIG
distribution.

The quadrature is evaluated on a log-\eqn{W} scale centered near the mode
of the updated GIG distribution. This is more stable than integrating
directly over \eqn{(0,\infty)} when the sequential posterior is highly
concentrated.

If \code{parameters} were estimated from \code{draws}, the returned values are not
exactly independent uniforms. A formal goodness-of-fit p-value therefore
requires a fitted-parameter calibration.
}
\examples{
set.seed(1)
mu <- array(0, dim = c(2, 2))
sigmas <- list(diag(2), diag(2))
draws <- rtnorm(10, mu = mu, sigmas = sigmas)

uniforms <- tensor_rosenblatt(
  draws,
  model = "normal",
  parameters = list(mu = mu, sigmas = sigmas)
)
uniforms
}
